--- a/src/calc.ts
+++ b/src/calc.ts
@@ -34,7 +34,7 @@
 export function range(start: number, end: number): number[] {
   const result: number[] = [];
-  for (let i = start; i < end; i++) {
+  for (let i = start; i <= end; i++) {
     result.push(i);
   }
   return result;
